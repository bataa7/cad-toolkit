# 消息推送和增量更新系统 - 集成指南

## 📦 已创建的文件

### 核心模块
- `notification_system.py` - 消息推送系统（完整实现）
- `update_system.py` - 增量更新系统（完整实现）
- `system_config.py` - 系统配置文件

### 示例和测试
- `integration_example.py` - 完整的集成示例
- `test_notifications.json` - 测试通知数据
- `test_update_info.json` - 测试更新信息
- `test_server.py` - 本地测试服务器
- `启动测试.bat` - 快速启动脚本

### 文档
- `消息推送和更新系统说明.md` - 详细使用说明
- `集成指南.md` - 本文件

## 🚀 快速开始（3步集成）

### 第1步：测试功能

1. 安装依赖：
```bash
pip install flask requests
```

2. 启动测试服务器：
```bash
python test_server.py
# 或双击运行：启动测试.bat
```

3. 在另一个终端运行示例程序：
```bash
python integration_example.py
```

4. 测试功能：
   - 点击工具栏的"🔔 通知"按钮查看通知中心
   - 菜单栏 → 帮助 → 检查更新

### 第2步：集成到主程序

在 `cad_toolkit_gui.py` 的 `MainWindow` 类中添加以下代码：

#### 2.1 导入模块（文件开头）

```python
from notification_system import NotificationManager, NotificationWidget, NotificationFetcher
from update_system import UpdateChecker, UpdateDialog
from system_config import NOTIFICATION_CONFIG, UPDATE_CONFIG, APP_VERSION
```

#### 2.2 初始化（`__init__` 方法中）

```python
def __init__(self):
    super().__init__()
    # ... 现有代码 ...
    
    # 初始化通知管理器
    self.notification_manager = NotificationManager(
        api_url=NOTIFICATION_CONFIG['api_url'],
        cache_file=NOTIFICATION_CONFIG['cache_file']
    )
    
    self.init_ui()
    self.init_notification_system()  # 新增
    self.init_update_system()        # 新增
```

#### 2.3 添加通知小部件（`init_ui` 方法中）

```python
def init_ui(self):
    # ... 现有代码 ...
    
    # 在工具栏或状态栏添加通知小部件
    self.notification_widget = NotificationWidget(self.notification_manager, self)
    # 如果有工具栏：
    # toolbar.addWidget(self.notification_widget)
    # 或添加到状态栏：
    # self.statusBar().addPermanentWidget(self.notification_widget)
```

#### 2.4 添加菜单项（`create_menu` 方法中）

```python
def create_menu(self):
    # ... 现有代码 ...
    
    help_menu = menubar.addMenu('帮助(&H)')
    
    # 检查更新
    check_update_action = QAction('检查更新', self)
    check_update_action.triggered.connect(self.manual_check_update)
    help_menu.addAction(check_update_action)
    
    # 通知中心
    notification_action = QAction('通知中心', self)
    notification_action.triggered.connect(self.show_notification_center)
    help_menu.addAction(notification_action)
    
    help_menu.addSeparator()
    
    # ... 关于等其他菜单项 ...
```

#### 2.5 添加方法（在 `MainWindow` 类中）

```python
def init_notification_system(self):
    """初始化通知系统"""
    if not NOTIFICATION_CONFIG['enabled']:
        return
    
    # 首次获取通知
    self.fetch_notifications()
    
    # 定时检查
    self.notification_timer = QTimer(self)
    self.notification_timer.timeout.connect(self.fetch_notifications)
    self.notification_timer.start(NOTIFICATION_CONFIG['check_interval'] * 1000)

def fetch_notifications(self):
    """获取通知"""
    fetcher = NotificationFetcher(NOTIFICATION_CONFIG['api_url'])
    fetcher.notifications_received.connect(self.on_notifications_received)
    fetcher.start()

def on_notifications_received(self, notifications):
    """收到通知"""
    self.notification_manager.notifications = notifications
    if hasattr(self, 'notification_widget'):
        self.notification_widget.update_badge()

def show_notification_center(self):
    """显示通知中心"""
    from notification_system import NotificationDialog
    dialog = NotificationDialog(self.notification_manager, self)
    dialog.exec_()
    if hasattr(self, 'notification_widget'):
        self.notification_widget.update_badge()

def init_update_system(self):
    """初始化更新系统"""
    if not UPDATE_CONFIG['enabled']:
        return
    
    # 启动时检查更新（延迟3秒）
    if UPDATE_CONFIG['check_on_startup']:
        QTimer.singleShot(3000, self.check_update)
    
    # 定时检查
    self.update_timer = QTimer(self)
    self.update_timer.timeout.connect(self.check_update)
    self.update_timer.start(UPDATE_CONFIG['auto_check_interval'] * 1000)

def check_update(self, silent=True):
    """检查更新"""
    self.update_checker = UpdateChecker(
        api_url=UPDATE_CONFIG['api_url'],
        current_version=APP_VERSION
    )
    self.update_checker.update_available.connect(
        lambda info: self.on_update_available(info, silent)
    )
    self.update_checker.no_update.connect(
        lambda: self.on_no_update(silent)
    )
    self.update_checker.start()

def manual_check_update(self):
    """手动检查更新"""
    self.check_update(silent=False)

def on_update_available(self, update_info, silent):
    """有可用更新"""
    dialog = UpdateDialog(update_info, APP_VERSION, self)
    dialog.exec_()

def on_no_update(self, silent):
    """无可用更新"""
    if not silent:
        QMessageBox.information(self, "检查更新", "当前已是最新版本")
```

### 第3步：配置服务器

#### 选项A：使用本地测试服务器（开发测试）

在 `system_config.py` 中：
```python
NOTIFICATION_CONFIG = {
    "api_url": "http://localhost:5000/api/notifications",
    "enabled": True,
}

UPDATE_CONFIG = {
    "api_url": "http://localhost:5000/api/check_update",
    "enabled": True,
}
```

#### 选项B：使用生产服务器

1. 搭建Flask服务器（参考 `test_server.py`）
2. 部署到云服务器
3. 配置域名和HTTPS
4. 更新 `system_config.py` 中的API地址

#### 选项C：使用静态文件托管（最简单）

1. 将 `test_notifications.json` 和 `test_update_info.json` 上传到GitHub Pages或其他静态托管
2. 配置API地址为文件URL：
```python
NOTIFICATION_CONFIG = {
    "api_url": "https://username.github.io/cad-toolkit/notifications.json",
    "enabled": True,
}
```

## 📋 功能清单

### 消息推送系统 ✅
- [x] 从服务器获取通知
- [x] 多级别通知（信息/警告/严重/成功）
- [x] 未读标记和徽章
- [x] 通知中心界面
- [x] 本地缓存
- [x] 自动检查
- [x] HTML内容支持

### 增量更新系统 ✅
- [x] 版本检查
- [x] 增量更新（差分下载）
- [x] 完整包更新
- [x] 文件哈希校验
- [x] 自动备份恢复
- [x] 进度显示
- [x] 跳过版本
- [x] 自动重启

## 🎯 使用场景

### 消息推送
- 发布新版本通知
- 系统维护公告
- 重要功能更新
- 使用技巧分享
- 紧急安全警告

### 增量更新
- 小版本更新（只改几个文件）
- 快速修复bug
- 节省用户流量
- 提高更新速度
- 降低服务器带宽成本

## 🔧 自定义配置

### 修改检查频率

```python
# 通知检查间隔（秒）
NOTIFICATION_CONFIG['check_interval'] = 600  # 10分钟

# 更新检查间隔（秒）
UPDATE_CONFIG['auto_check_interval'] = 7200  # 2小时
```

### 禁用某个功能

```python
# 禁用通知系统
NOTIFICATION_CONFIG['enabled'] = False

# 禁用更新系统
UPDATE_CONFIG['enabled'] = False

# 禁用启动时检查更新
UPDATE_CONFIG['check_on_startup'] = False
```

### 自定义通知样式

编辑 `notification_system.py` 中的 `NotificationDialog` 类，修改HTML模板和CSS样式。

### 自定义更新逻辑

继承 `UpdateChecker` 或 `UpdateInstaller` 类，重写相关方法。

## 📊 监控和日志

### 查看日志

```python
import logging

# 启用详细日志
logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    filename='cad_toolkit.log'
)
```

### 统计信息

可以在服务器端记录：
- 通知查看次数
- 更新下载次数
- 版本分布
- 用户活跃度

## 🛡️ 安全建议

1. **使用HTTPS** - 生产环境必须使用HTTPS
2. **验证哈希** - 始终验证下载文件的完整性
3. **备份机制** - 更新前自动备份
4. **权限控制** - 确保有文件写入权限
5. **错误处理** - 完善的异常处理和恢复机制

## 🐛 常见问题

### Q: 通知无法获取？
A: 检查网络连接和API地址，确保服务器可访问。

### Q: 更新失败怎么办？
A: 系统会自动恢复备份，也可以手动从备份目录恢复。

### Q: 如何测试增量更新？
A: 修改 `test_update_info.json` 中的版本号，重启程序即可触发更新检查。

### Q: 可以离线使用吗？
A: 可以，离线时只是无法获取新通知和检查更新，不影响主要功能。

## 📞 技术支持

如有问题，请查看：
1. `消息推送和更新系统说明.md` - 详细文档
2. `integration_example.py` - 完整示例代码
3. 日志文件 - 查看错误信息

## 🎉 完成！

现在你的CAD工具包已经具备了：
- ✅ 消息推送功能
- ✅ 增量更新功能
- ✅ 完整的测试环境
- ✅ 详细的文档

开始使用吧！
